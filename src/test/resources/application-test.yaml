spring:
  application:
    name: recoverAiBackend
  datasource:
    url: jdbc:postgresql://${DB_HOST:vitaloop.app}:${DB_PORT:5432}/${DB_NAME:vitaloop}
    username: ${DB_USERNAME:vitaloop-admin}
    password: ${DB_PASSWORD:e898283a-2096-4094-b667-7e406f4f8a0f}
    hikari:
      maximum-pool-size: ${DB_POOL_SIZE:10}
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      validation-timeout: 5000
      auto-commit: true

  jpa:
    show-sql: ${DB_SHOW_SQL:true}
    hibernate:
      ddl-auto: ${DB_DDL_AUTO:update}
    open-in-view: true

jwt:
  secret: ${JWT_SECRET_KEY:fbd8f9b3aecd0dbacd6774285077ae0301bbbf7649a954572a854555f87ee40b}
